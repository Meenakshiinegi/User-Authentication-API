# User-Authentication-API
User Authentication API is a Backend RESTful application built using Spring Boot that provides secure user authentication and authorization using JWT (JSON Web Tokens). The project follows standard Backend development practices and demonstrates how stateless authentication can be implemented in real-world applications.
